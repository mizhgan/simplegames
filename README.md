# SimpleGames

Сайт с простыми браузерными играми. Чистый HTML, CSS и JavaScript: без сборки, бэкенда и базы данных.

## Игры

| Игра | Папка | Управление |
| --- | --- | --- |
| Змейка | `games/snake/` | стрелки / WASD, свайпы, экранные кнопки |
| 2048 | `games/2048/` | стрелки / WASD, свайпы, отмена хода |
| Крестики-нолики | `games/tictactoe/` | мышь / касание, против компьютера (3 уровня), вдвоём или по сети |
| Мемори | `games/memory/` | мышь / касание, поля 4×4, 5×4, 6×6 |
| Сапёр | `games/minesweeper/` | ЛКМ — открыть, ПКМ / долгое нажатие — флажок |
| Арканоид | `games/breakout/` | мышь, палец, стрелки / AD |
| Тетрис | `games/tetris/` | стрелки, пробел, Z / X / C, жесты и кнопки на телефоне |
| Птичка | `games/flappy/` | пробел / клик / касание |
| Судоку | `games/sudoku/` | мышь / касание, цифры с клавиатуры, заметки, подсказки |
| Пятнашки | `games/fifteen/` | клик по плитке, стрелки, свайпы; поля 3×3, 4×4, 5×5 |
| Четыре в ряд | `games/connect4/` | клик по столбцу / клавиши 1–7, против компьютера, вдвоём или по сети |
| Вордли | `games/wordle/` | экранная или обычная клавиатура (в т. ч. английская раскладка); слово дня и свободная игра |
| Косынка | `games/solitaire/` | перетаскивание или клик по карте, отмена, по 1 или 3 карты |
| Шашки | `games/checkers/` | русские правила, против компьютера (3 уровня), вдвоём или по сети |
| Динозаврик | `games/dino/` | пробел / ↑ — прыжок, ↓ — пригнуться, касание и экранные кнопки |
| Линии | `games/lines/` | клик по шару и клетке, отмена хода, автосохранение |
| Паук | `games/spider/` | 1, 2 или 4 масти, перетаскивание или клик, отмена |
| Реверси | `games/reversi/` | против компьютера (3 уровня), вдвоём или по сети, подсказки ходов |
| Морской бой | `games/battleship/` | классический флот, против компьютера (3 уровня) или по сети |
| Три в ряд | `games/match3/` | клик по двум камням или свайп, 30 ходов, подсказка |
| Stack | `games/stack/` | клик / касание / пробел |
| Дурак | `games/durak/` | подкидной, колода 36 карт, против компьютера или по сети |
| Нарды | `games/nardy/` | длинные нарды против компьютера или по сети |
| Маджонг | `games/mahjong/` | раскладка «черепаха», подсказка, перемешивание, отмена |
| Шарики | `games/bubbles/` | Bubble Shooter: прицел мышью / пальцем / стрелками |
| Филворд | `games/fillword/` | поля 5×5 … 8×8, выделение слов протягиванием |
| Пакман | `games/pacman/` | стрелки / WASD, свайпы, экранные кнопки |
| Балда | `games/balda/` | против компьютера (3 уровня) или по сети, словарь 50 тыс. существительных |
| Свободная ячейка | `games/freecell/` | FreeCell: перетаскивание или клик, отмена, автоперенос в дом |
| Японские кроссворды | `games/nonogram/` | 12 картинок и случайные поля 5×5 … 15×15, все решаются логикой |
| Прыгун | `games/jumper/` | стрелки, касание половин экрана или наклон телефона |
| Понг | `games/pong/` | W/S и стрелки, мышь или касание; против компьютера (3 уровня), вдвоём или по сети |
| Саймон | `games/simon/` | клик / касание, клавиши 1–4 |
| Виселица | `games/hangman/` | экранная или обычная клавиатура |
| Слова из слова | `games/wordsmith/` | клик по буквам или ввод с клавиатуры, подсказки |
| Гомоку | `games/gomoku/` | поле 15×15, против компьютера (3 уровня), вдвоём или по сети, отмена хода |
| Точки и квадраты | `games/dots/` | поля 3×3 … 5×5, против компьютера (3 уровня), вдвоём или по сети |
| Двадцать одно | `games/blackjack/` | блэкджек: ставки, «ещё», «хватит», «удвоить»; H / S / D |
| Покер на костях | `games/yahtzee/` | Yahtzee против компьютера, в одиночку или по сети; пробел, 1–5 |
| Соедини точки | `games/flow/` | протягивание линий, поля 5×5 … 9×9, подсказка |
| Водопроводчик | `games/pipes/` | клик — поворот трубы, поля 5×5, 7×7, 9×9 |
| Сокобан | `games/sokoban/` | стрелки / WASD, свайпы, клик по клетке; 32 уровня, отмена |
| Какуро | `games/kakuro/` | генератор с единственным решением, 3 размера, подсказка наборов цифр |
| Киллер-судоку | `games/killer/` | генератор с единственным решением, заметки, подсказки |
| Лягушка | `games/frogger/` | стрелки / WASD, свайпы, экранные кнопки |
| Космические захватчики | `games/invaders/` | ← → и пробел, экранные кнопки |
| Астероиды | `games/asteroids/` | ← → ↑, пробел, ↓ — гиперпрыжок, экранные кнопки |
| Яйцелов | `games/eggs/` | Q / A / P / L или стрелки, касание четверти поля |
| Танчики | `games/tanks/` | стрелки / WASD, пробел, экранные кнопки; 5 карт, бонусы |
| Шахматы | `games/chess/` | все правила (рокировка, взятие на проходе, превращение, ничьи), компьютер 3 уровней, запись ходов, отмена; против компьютера, вдвоём или по сети |
| Ultimate крестики-нолики | `games/uttt/` | девять полей 3×3; компьютер на Монте-Карло; против компьютера, вдвоём или по сети |
| Кватро | `games/quarto/` | 16 фигур с 4 признаками, фигуру для хода выбирает соперник; против компьютера, вдвоём или по сети |
| Уголки | `games/ugolki/` | 9 шашек 3×3, шаги и цепочки прыжков, правило 40 ходов; против компьютера, вдвоём или по сети |
| Мельница | `games/mill/` | расстановка, ходы, «полёт» с тремя фишками, снятие фишек; против компьютера, вдвоём или по сети |
| Манкала | `games/mancala/` | калах 6×4: дополнительный ход и захват; против компьютера, вдвоём или по сети |
| Гекс | `games/hex/` | поле 9×9, компьютер на UCT с RAVE; против компьютера, вдвоём или по сети |
| Го 9×9 | `games/go/` | китайский подсчёт, коми 6,5, правило ко, пас; против компьютера, вдвоём или по сети |
| Эрудит | `games/erudit/` | скрэббл 15×15 с премиальными клетками, обмен фишек, ввод с клавиатуры; против компьютера, вдвоём или по сети |
| Дуэль в Вордли | `games/wordduel/` | одно слово на двоих: кто угадает за меньшее число попыток; против компьютера или по сети |
| Города | `games/cities/` | ~1900 городов, 60 секунд на ход; против компьютера, вдвоём или по сети |
| Крокодил-рисовалка | `games/crocodile/` | холст с цветами и толщиной; компанией или по сети с проверкой ответов |
| Тысяча | `games/thousand/` | вдвоём: торговля, прикуп, марьяжи, до 1000; против компьютера или по сети |
| Сека | `games/seka/` | три карты, «шаха», торговля фишками, свара; против компьютера или по сети |
| Пьяница | `games/war/` | 36 карт, спор, автоигра; против компьютера, вдвоём или по сети |
| Покер: холдем | `games/holdem/` | один на один, растущие блайнды, рейзы ползунком, ва-банк; против компьютера или по сети |
| Трон | `games/tron/` | световые мотоциклы, до 3 раундов; стрелки / WASD, экранный джойстик; против компьютера, вдвоём или по сети |
| Аэрохоккей | `games/airhockey/` | бита мышью или пальцем, до 7 голов; против компьютера, вдвоём или по сети |
| Танчики вдвоём | `games/tankduel/` | дуэль с разрушаемыми стенами; WASD + пробел / стрелки + Enter; против компьютера, вдвоём или по сети |
| Бомбермен-дуэль | `games/bomber/` | бомбы, кирпичи, усилители, до 3 раундов; против компьютера, вдвоём или по сети |

Кроме игр на сайте есть:

- светлая и тёмная тема (по умолчанию берётся тема системы, выбор запоминается);
- поиск и фильтр игр по категориям на главной;
- избранные игры (звёздочка на карточке) и блок «Вы недавно играли»;
- «Игра дня»: каждый день новое задание в одной из игр и серия дней подряд;
- достижения (`achievements.html`) с уведомлениями при получении;
- игра по сети с другом (WebRTC) в 32 играх — все игры на двоих, включая карточные и игры в реальном времени; счёт серии, реванш, реакции, список открытых комнат «Ищу соперника» и зрители (до 10 на партию) по ссылке «👁 Зрителям»;
- офлайн-режим: сайт устанавливается как приложение (PWA) и работает без интернета;
- звуковые эффекты с кнопкой вкл/выкл в шапке (по умолчанию выключены): звуки синтезируются через Web Audio API, аудиофайлов нет;
- адаптивная вёрстка под телефоны, планшеты и десктоп;
- рекорды в `localStorage` браузера, они же показываются на карточках главной страницы.

Словари «Вордли» (`games/wordle/words.js`), «Филворда» и «Виселицы» (`sg/data/nouns.js`), «Балды» и «Слов из слова» (`sg/data/balda-dict.js`) собраны из морфологического словаря OpenCorpora (pymorphy3): существительные в начальной форме. Используются частотные слова (по данным wordfreq), из них вручную убраны имена собственные и неуместные слова.

## Структура

```
index.html          главная страница
404.html            страница «не найдено»
sg/css/style.css    общие стили и темы
achievements.html   страница достижений
sg/css/cards.css    стили игральных карт
sg/js/common.js     общие утилиты (тема, рекорды, звук, свайпы)
sg/js/site.js       каталог игр, недавние и избранные, достижения, «Игра дня»
sg/js/net.js        игра по сети (WebRTC): комнаты, лобби, реванш, реакции
sg/js/duel.js       каркас пошаговых игр на двоих (компьютер / вдвоём / по сети) + альфа-бета и Монте-Карло
sg/js/rt.js         каркас игр в реальном времени на двоих (физика у хозяина, гость шлёт нажатия)
sg/js/cards.js      колоды и вид карт для карточных игр на двоих
sg/css/cardtable.css  стол карточных игр на двоих
sg/vendor/          PeerJS (MIT) — загружается, только когда выбрана игра по сети
sg/data/            общие словари
sg/img/             иконки (в т. ч. PNG для установки приложения)
manifest.webmanifest  описание приложения для установки (PWA)
sw.js               service worker для офлайн-режима (генерируется)
tools/build-sw.py   сборка sw.js
games/<игра>/       index.html + game.js (+ style.css) каждой игры
```

## Развёртывание

Скопируйте содержимое репозитория в корень сайта на хостинге (обычно `public_html`, `www` или `/var/www/<домен>`), например:

```bash
rsync -av --delete --exclude '.git' ./ user@host:/var/www/example.com/
```

Больше ничего не нужно: сервер просто отдаёт статические файлы.

- **Apache**: `.htaccess` уже включает `index.html`, страницу 404, сжатие и кэширование.
- **nginx**: минимальный конфиг:

  ```nginx
  server {
      server_name example.com;
      root /var/www/example.com;
      index index.html;
      error_page 404 /404.html;
      location / { try_files $uri $uri/ =404; }
  }
  ```

- Подойдёт и любой статический хостинг: GitHub Pages, Netlify, Cloudflare Pages и т. п.

Все ссылки между страницами относительные, поэтому сайт работает и из подпапки, и при открытии `index.html` прямо с диска. Исключение — `404.html`: в ней абсолютные пути от корня домена, потому что сервер может отдать её по любому адресу.

## Локальный запуск

```bash
python3 -m http.server 8000
# затем откройте http://localhost:8000
```

## Офлайн-режим

`sw.js` кеширует все файлы сайта при первом визите, поэтому игры открываются и без интернета, а сайт можно установить на телефон или компьютер как приложение. Страницы загружаются «сначала из сети», поэтому обновления видны сразу.

После добавления или изменения файлов пересоберите список кешируемых файлов:

```bash
python3 tools/build-sw.py
```

Офлайн-режим работает только по HTTPS (или на `localhost`).

## Игра по сети

В пошаговых играх на двоих есть режим «🌐 По сети». Один игрок нажимает его и отправляет другу ссылку (или код комнаты), друг открывает её — и игра начинается. Ходы передаются напрямую между браузерами по WebRTC, сервер сайта для этого не нужен.

- Чтобы браузеры нашли друг друга, используется бесплатный публичный сервер знакомств PeerJS (`0.peerjs.com`) и его TURN-серверы — через них идёт только обмен приглашениями, а если прямое соединение невозможно, TURN пересылает трафик.
- Если сервер знакомств недоступен, включается ручной режим: хозяин отправляет ссылку-приглашение, гость присылает в ответ код, хозяин вставляет его — без каких-либо серверов, кроме STUN.
- Бесплатные публичные серверы PeerJS работают без гарантий. Если игрокам не удаётся соединиться (чаще всего в мобильном интернете, где прямое соединение невозможно), в окне ошибки видна диагностика: дошли ли до сервера, ответил ли друг, удалось ли прямое соединение и есть ли ретранслятор (TURN).
- Для надёжной игры поднимите на своём сервере TURN ([coturn](https://github.com/coturn/coturn)) и, при желании, свой сервер знакомств ([peerjs-server](https://github.com/peers/peerjs-server)) и пропишите их в блоке `CONFIG` в начале `sg/js/net.js` (`iceServers` и `peerServer`). Для проверки без правки файлов сервер знакомств можно задать в браузере: `localStorage['sg:peer-server'] = '{"host":"peer.example.com","port":443,"path":"/","secure":true}'`.
- Чтобы добавить сеть в игру: `SG.net.setup({ game, modeEl, onConnect(role), onMessage(msg), onDisconnect(voluntary), onRematch() })`, затем `net.send({...})` на каждый ход и `net.result('win' | 'lose' | 'draw')` в конце партии (счёт серии и кнопка «Реванш»). Хозяин (`host`) задаёт параметры партии, гость (`guest`) их принимает.
- Новые пошаговые игры проще делать на каркасе `SG.duel` (`sg/js/duel.js`): игра описывает правила (`create`, `legal`, `apply`, `over`) и отрисовку, а режимы, очередь ходов, счёт, отмена хода и сеть — общие. Раздача карт и фишек идёт от общего «зерна», поэтому у обоих игроков по сети всё совпадает. Игры в реальном времени — на `SG.rt` (`sg/js/rt.js`).
- **Зрители.** По ссылке `#watch=код` (кнопка «👁 Зрителям» в полоске игры и в окне приглашения, есть и у хозяина, и у гостя) до 10 человек смотрят партию, не играя. Зрители подключаются к хозяину, он пересылает им ходы. В играх на `SG.duel` и `SG.rt` зритель видит нейтральную картину (карты и фишки в руках закрыты), в остальных — «зеркало» экрана хозяина, где его секреты (рука, расстановка флота, буквы, загаданное слово) спрятаны через `opts.mirrorMask`.
- Комнаты на сервере знакомств записаны под хэшем кода, а гость называет сам код при подключении — так список комнат на сервере не раскрывает приглашения. Комнату можно показать всем («Ищу соперника»): она появится в окне приглашения у других игроков той же игры. Для этого свой сервер знакомств должен отдавать список (`--allow_discovery`, уже включено в `deploy/peerjs/install.sh` — перезапустите скрипт, если ставили раньше).

### Свой TURN-сервер (coturn)

Без ретранслятора браузеры часто не могут соединиться, если кто-то из игроков в мобильном интернете: в окне ошибки это видно по строке «ретранслятор TURN: недоступен». Бесплатный TURN от PeerJS работает с перебоями, поэтому лучше поставить свой — нагрузка от пошаговых игр мизерная, хватит самой маленькой VPS.

**Установка одной командой** (чистая VPS с Ubuntu 22.04+/Debian 12+, от root). Заранее создайте DNS-запись A для поддомена (например, `turn.example.com`) на IP этой VPS:

```bash
curl -fsSLO https://raw.githubusercontent.com/mizhgan/simplegames/main/deploy/coturn/install.sh
bash install.sh turn.example.com you@example.com
```

Скрипт поставит coturn, получит сертификат Let's Encrypt (TURN по TLS на порту 443 проходит даже через строгие файрволы), настроит ограничения и запрет пересылки во внутренние сети, откроет порты в ufw и напечатает строку для `CONFIG.iceServers` в `sg/js/net.js`. Если у провайдера VPS есть свой файрвол, откройте в нём `80/tcp`, `443/tcp`, `3478/tcp+udp` и `49160–49250/udp`.

Вручную: `apt install coturn`, конфиг-образец — `deploy/coturn/turnserver.conf`.

**Проверка ретранслятора.** Откройте на сайте страницу `turn-test.html`: она соединит два канала в одном браузере только через ваш TURN (как у игроков, которым не удалось связаться напрямую) и покажет, на каком шаге сбой. Если адреса выдаются, а соединение не устанавливается — не открыт диапазон `49160–49250/udp` в файрволе хостера или VPS стоит за NAT (свежий `install.sh` это определяет и прописывает `external-ip=ВНЕШНИЙ/ВНУТРЕННИЙ`).

**Проверка до публикации.** Откройте игру, в консоли браузера выполните
`localStorage['sg:ice-servers'] = JSON.stringify([{urls: 'turn:turn.example.com:3478', username: 'simplegames', credential: 'пароль'}]); localStorage['sg:ice-policy'] = '"relay"'`
и обновите страницу (у друга — то же самое): соединение пойдёт только через ваш TURN. Отменить: `localStorage.removeItem('sg:ice-servers'); localStorage.removeItem('sg:ice-policy')`. Ещё один способ — страница [Trickle ICE](https://webrtc.github.io/samples/src/content/peerconnection/trickle-ice/): должен появиться кандидат типа `relay`.

### Свой сервер знакомств (peerjs-server)

По умолчанию игроки находят друг друга через бесплатный публичный `0.peerjs.com`. Чтобы не зависеть от него, поставьте свой сервер — можно на ту же VPS, что и coturn:

```bash
curl -fsSLO https://raw.githubusercontent.com/mizhgan/simplegames/main/deploy/peerjs/install.sh
bash install.sh            # домен возьмётся из сертификата coturn, порт 8443
```

Скрипт поставит Node.js (если нужно) и `peer`, запустит службу `peerjs` (со списком открытых комнат `--allow_discovery` для лобби «Ищу соперника») с TLS-сертификатом Let's Encrypt (продлевается автоматически) и напечатает строку для `CONFIG.peerServer` в `sg/js/net.js`. Порт 443 на этой VPS занят TURN, поэтому сервер слушает 8443 — откройте его и в файрволе хостера. Проверка: `https://домен:8443/peerjs/id` возвращает случайную строку.

Если свой сервер недоступен, сайт сам создаст комнату на публичном — к коду в ссылке добавится `-p`, чтобы гость пришёл туда же.

## Как добавить новую игру

1. Скопируйте папку любой игры, например `games/snake/`, в `games/<новая>/`.
2. Поменяйте заголовок, описание и `game.js`. Общие функции доступны через `window.SG`: `SG.store`, `SG.sound.play('win')` (список звуков — в `PRESETS` в `common.js`), `SG.onSwipe`, `SG.touchKeys` (экранные кнопки аркад), `SG.segmented`, `SG.formatTime`, `SG.shuffle`, `SG.cssVar`.
3. Добавьте карточку игры в `index.html` и название в `GAMES` в `sg/js/site.js`; подключите `sg/js/site.js` после `common.js`.
4. Запустите `python3 tools/build-sw.py`, чтобы игра попала в офлайн-кеш.
